\* the configuration file for rwlock.tla
SPECIFICATION Spec

\* validate the invariants we wrote in PlusCal macros and definitions
INVARIANTS
    SuccessionProperty

\* check whether will enter deadlock status
\* in the concurrency lock model, this check is important as wrong locking order may lead to deadlock
CHECK_DEADLOCK TRUE